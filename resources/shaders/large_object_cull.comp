#version 460 core

layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

struct DrawElementsIndirectCommand {
    uint count;
    uint instanceCount;
    uint firstIndex;
    uint baseVertex;
    uint baseInstance;
};

struct MeshInfo {
    uint indexCount;
    uint firstIndex;
    uint baseVertex;
    float boundingRadius;
    vec4 boundingCenter;
};

struct RenderableComponent {
    uint mesh_uuid;
    uint material_uuid;
    uint shaderId;
    uint objectId;
    float alpha;
};

layout (std140) uniform SceneUniforms {
    mat4 projection;
    mat4 view;
    vec3 lightPos;
    vec3 viewPos;
    vec3 lightColor;
    vec4 frustumPlanes[6];
} sceneData;

layout(std140) uniform LargeObjectCullConstants {
    uint u_objectCount;
    uint u_maxDraws;
    float u_largeObjectThreshold;
    uint u_numShaders;
};

// Per-(shaderId, mesh_uuid) survivor counts. The depth pre-pass consumes these through the same
// prefix-sum/scatter/command-gen chain the opaque pass uses, so it can issue one instanced draw
// per bucket instead of one indirect draw per visible large object.
layout(std430) buffer LargeBucketCountBuffer {
    uint largeBucketCounts[];
};

layout(std430) buffer VisibleLargeObjectAtomicCounter {
    uint visibleLargeObjectCount;
};

layout(std430) buffer VisibleLargeObjectBuffer {
    uvec2 visibleLargeObjects[];
};

layout(std430) readonly buffer CullSphereBuffer {
    vec4 cullSpheres[];
};

layout(std430) readonly buffer RenderableBuffer {
    RenderableComponent renderables[];
};

// Must match MAX_MESHES in Renderer.cpp - meshInfos[] is sized by it.
const uint MAX_MESHES = 2048;

// the pre-pass only builds the Hi-Z pyramid, so occluders use a coarse LOD (inside the surface for convex shapes, hence conservative)
const uint OCCLUDER_LOD = 2;

const float FRUSTUM_PADDING_FACTOR = 1.05f;

bool isVisible(vec3 world_pos, float radius) {
    for (int i = 0; i < 6; i++) {
        if (dot(sceneData.frustumPlanes[i].xyz, world_pos) + sceneData.frustumPlanes[i].w < -radius) {
            return false;
        }
    }
    return true;
}

void main() {
    uint objectId = gl_GlobalInvocationID.x;
    if (objectId >= u_objectCount) return;

    vec4 boundingSphere = cullSpheres[objectId];
    if (boundingSphere.w < 0.0) return;
    vec3 world_pos = boundingSphere.xyz;
    float world_radius = boundingSphere.w;

    if (!isVisible(world_pos, world_radius * FRUSTUM_PADDING_FACTOR)) return;

    float dist = distance(world_pos, sceneData.viewPos);
    if (dist > 0.0 && world_radius / dist < u_largeObjectThreshold) return;

    RenderableComponent renderable = renderables[objectId];
    if (renderable.alpha < 1.0) return;
    if (renderable.mesh_uuid >= MAX_MESHES || renderable.shaderId >= u_numShaders) return;

    uint index = atomicAdd(visibleLargeObjectCount, 1);

    if (index < u_maxDraws) {
        uint bucketKey = renderable.shaderId * MAX_MESHES + renderable.mesh_uuid + OCCLUDER_LOD;
        visibleLargeObjects[index] = uvec2(objectId | (OCCLUDER_LOD << 29u), bucketKey);
        atomicAdd(largeBucketCounts[bucketKey], 1);
    }
}

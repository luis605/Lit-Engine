#version 450

layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

struct RenderableComponent {
    uint mesh_uuid;
    uint material_uuid;
    uint shaderId;
    uint objectId;
    float alpha;
};

struct MeshInfo {
    uint indexCount;
    uint firstIndex;
    uint baseVertex;
    float boundingRadius;
    vec4 boundingCenter;
};

layout(std140) uniform SceneData {
    mat4 projection;
    mat4 view;
    vec3 lightPos;
    vec3 viewPos;
    vec3 lightColor;
    vec4 frustumPlanes[6];
    vec4 dirLightDir;
    vec4 dirLightColor;
    vec4 pointLight0Pos;
    vec4 pointLight0Color;
    vec4 pointLight1Pos;
    vec4 pointLight1Color;
};

layout(std140) uniform CullingUniforms {
    uint u_objectCount;
    uint u_maxDraws;
    uint u_baseIndex;
    float u_smallObjectThreshold;
    float u_hizMaxMipLevel;
    float u_hizTextureSizeX;
    float u_hizTextureSizeY;
    uint u_numShaders;
};

layout(std430) buffer AtomicCounterBuffer {
    uint u_atomicCounter;
};

layout(std430) writeonly buffer VisibleObjectBuffer {
    uint u_visibleObjects[];
};

layout(std430) readonly buffer WorldMatrixBuffer {
    mat4 worldMatrices[];
};

layout(std430) readonly buffer MeshInfoBuffer {
    MeshInfo meshInfos[];
};

layout(std430) readonly buffer RenderableBuffer {
    RenderableComponent renderables[];
};

layout(std430) buffer BucketCountBuffer {
    uint bucketCounts[];
};

uniform sampler2D u_hizTexture;

void main() {
    uint id = gl_GlobalInvocationID.x;
    if (id >= u_objectCount) return;

    RenderableComponent r = renderables[id];
    uint baseMeshUuid = r.mesh_uuid;
    MeshInfo mesh = meshInfos[baseMeshUuid];
    mat4 model = worldMatrices[id];

    vec3 worldCenter = (model * vec4(mesh.boundingCenter.xyz, 1.0)).xyz;
    float scaleX = length(model[0].xyz);
    float scaleY = length(model[1].xyz);
    float scaleZ = length(model[2].xyz);
    float maxScale = max(scaleX, max(scaleY, scaleZ));
    float radius = mesh.boundingRadius * maxScale;

    for (int i = 0; i < 6; ++i) {
        if (dot(frustumPlanes[i].xyz, worldCenter) + frustumPlanes[i].w < -radius) return;
    }

    vec4 viewPosCenter = view * vec4(worldCenter, 1.0);
    float dist = -viewPosCenter.z;
    if (dist <= 0.0) dist = 0.001;
    float pRadius = radius / dist;
    if (pRadius < u_smallObjectThreshold) return;

    if (viewPosCenter.z + radius < -0.1) {
        vec4 clipCenter = projection * vec4(viewPosCenter.xyz, 1.0);
        vec2 ndcCenter = clipCenter.xy / clipCenter.w;
        vec2 pSize = vec2(radius * abs(projection[0][0]), radius * abs(projection[1][1])) / dist;

        vec2 uvCenter = vec2(ndcCenter.x * 0.5 + 0.5, 0.5 - ndcCenter.y * 0.5);

        float u0 = clamp(uvCenter.x - pSize.x * 0.5, 0.0, 1.0);
        float u1 = clamp(uvCenter.x + pSize.x * 0.5, 0.0, 1.0);
        float v0 = clamp(uvCenter.y - pSize.y * 0.5, 0.0, 1.0);
        float v1 = clamp(uvCenter.y + pSize.y * 0.5, 0.0, 1.0);

        vec2 uvMin = vec2(min(u0, u1), min(v0, v1));
        vec2 uvMax = vec2(max(u0, u1), max(v0, v1));

        vec2 sizePixels = (uvMax - uvMin) * vec2(u_hizTextureSizeX, u_hizTextureSizeY);
        float maxPix = max(sizePixels.x, sizePixels.y);
        float mip = clamp(ceil(log2(max(maxPix, 1.0))), 0.0, u_hizMaxMipLevel);

        float d0 = textureLod(u_hizTexture, vec2(uvMin.x, uvMin.y), mip).r;
        float d1 = textureLod(u_hizTexture, vec2(uvMax.x, uvMin.y), mip).r;
        float d2 = textureLod(u_hizTexture, vec2(uvMin.x, uvMax.y), mip).r;
        float d3 = textureLod(u_hizTexture, vec2(uvMax.x, uvMax.y), mip).r;
        float maxHizDepth = max(max(d0, d1), max(d2, d3));

        vec4 nearPos = viewPosCenter;
        nearPos.z += radius;
        vec4 clipNear = projection * nearPos;
        float sphereNearDepth = clipNear.z / clipNear.w;

        if (sphereNearDepth > maxHizDepth) return;
    }

    uint chosenLod = 0;
    if (mesh.indexCount > 36) {
        float distToCam = distance(worldCenter, viewPos);
        if (distToCam > 280.0) chosenLod = 3;
        else if (distToCam > 170.0) chosenLod = 2;
        else if (distToCam > 85.0) chosenLod = 1;
    }

    uint chosenMesh = baseMeshUuid + chosenLod;

    uint slot = atomicAdd(u_atomicCounter, 1);
    if (slot < u_maxDraws) {
        u_visibleObjects[slot] = id | (chosenLod << 30u);
    }

    uint bucketIndex = r.shaderId * 2048 + chosenMesh;
    atomicAdd(bucketCounts[bucketIndex], 1);
}